{
  "name": "mpa",
  "version": "1.0.0",
  "license": "MIT",
  "description": "A full-featured Webpack setup with hot-reload, lint-on-save, unit testing & css extraction.",
  "scripts": {
    "test": "rm -rf ./test && ./node_modules/.bin/vue init . test && cd test && npm i"
  },
  "devDependencies": {
    "vue-cli": "^2.8.1"
  }
}
